# Copyright 2024 Canonical Ltd.
# See LICENSE file for licensing details.

# Usage documentation: build_rock.md

on:
  workflow_call:
    inputs:
      artifact-prefix:
        description: Rock packages are uploaded to GitHub artifacts beginning with this prefix
        default: packed-rock
        type: string
      path-to-rock-directory:
        description: Relative path to rock directory from repository directory
        default: .
        type: string
      rockcraft-snap-revisions:
        description: |
          JSON string with type dict[str, str] of architecture to rockcraft snap revision

          Example: `{"amd64": "16585", "arm64": "16587"}`
        required: false
        type: string
      rockcraft-snap-channel:
        description: |
          rockcraft snap channel

          Cannot be used if `rockcraft-snap-revisions` input is passed
        required: false
        type: string
      lxd-snap-revisions:
        description: |
          JSON string with type dict[str, str] of architecture to LXD snap revision

          Example: `{"amd64": "16585", "arm64": "16587"}`

          LXD from base runner image will be used if neither `lxd-snap-revisions` or `lxd-snap-channel` is passed
        required: false
        type: string
      lxd-snap-channel:
        description: |
          LXD snap channel

          Cannot be used if `lxd-snap-revisions` input is passed

          LXD from base runner image will be used if neither `lxd-snap-revisions` or `lxd-snap-channel` is passed
        required: false
        type: string
    outputs:
      artifact-prefix:
        description: Rock packages are uploaded to GitHub artifacts beginning with this prefix
        value: ${{ inputs.artifact-prefix }}

jobs:
  collect-platforms:
    name: Collect platforms for rock | ${{ inputs.path-to-rock-directory }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Get workflow version
        id: workflow-version
        uses: canonical/get-workflow-version-action@v1
        with:
          repository-name: canonical/data-platform-workflows
          file-name: build_rock.yaml
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Install CLI
        run: pipx install git+https://github.com/canonical/data-platform-workflows@'${{ steps.workflow-version.outputs.sha }}'#subdirectory=_cli
      - name: Checkout
        uses: actions/checkout@v5
      - name: Collect rock platforms to build from rockcraft.yaml
        id: collect
        run: collect-rock-platforms --directory='${{ inputs.path-to-rock-directory }}'
    outputs:
      platforms: ${{ steps.collect.outputs.platforms }}

  build:
    strategy:
      matrix:
        platform: ${{ fromJSON(needs.collect-platforms.outputs.platforms) }}
    name: 'Build rock | ${{ matrix.platform.name }}'
    needs:
      - collect-platforms
    runs-on: ${{ matrix.platform.runner }}
    timeout-minutes: 15
    steps:
      - name: (IS hosted) Install pipx
        if: ${{ contains(matrix.platform.runner, 'self-hosted') }}
        run: |
          sudo apt-get update
          sudo apt-get install pipx -y
          pipx ensurepath
      - name: Get workflow version
        id: workflow-version
        uses: canonical/get-workflow-version-action@v1
        with:
          repository-name: canonical/data-platform-workflows
          file-name: build_rock.yaml
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Install CLI
        run: pipx install git+https://github.com/canonical/data-platform-workflows@'${{ steps.workflow-version.outputs.sha }}'#subdirectory=_cli
      - name: Parse rockcraft version inputs
        id: rockcraft-snap-version
        run: parse-snap-version --revisions='${{ inputs.rockcraft-snap-revisions }}' --channel='${{ inputs.rockcraft-snap-channel }}' --revision-input-name=rockcraft-snap-revisions --channel-input-name=rockcraft-snap-channel
      - name: Parse LXD version inputs
        id: lxd-snap-version
        run: parse-snap-version --revisions='${{ inputs.lxd-snap-revisions }}' --channel='${{ inputs.lxd-snap-channel }}' --revision-input-name=lxd-snap-revisions --channel-input-name=lxd-snap-channel
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up environment
        run: |
          sudo snap install lxd ${{ steps.lxd-snap-version.outputs.install_flag }}
          # shellcheck disable=SC2078
          # (shellcheck sees it as constant, but GitHub Actions expression is not constant between workflow runs)
          if [[ '${{ steps.lxd-snap-version.outputs.install_flag }}' ]]
          then
            sudo snap refresh lxd ${{ steps.lxd-snap-version.outputs.install_flag }}
          fi
          sudo adduser "$USER" lxd
          # `newgrp` does not work in GitHub Actions; use `sudo --user` instead
          sudo --user "$USER" --preserve-env --preserve-env=PATH -- env -- lxd waitready
          sudo --user "$USER" --preserve-env --preserve-env=PATH -- env -- lxd init --auto
          # Workaround for Docker & LXD on same machine
          sudo iptables -F FORWARD
          sudo iptables -P FORWARD ACCEPT

          sudo snap install rockcraft --classic ${{ steps.rockcraft-snap-version.outputs.install_flag }}
      - run: snap list
      - name: Pack rock
        id: pack
        working-directory: ${{ inputs.path-to-rock-directory }}
        run: sudo --user "$USER" --preserve-env --preserve-env=PATH -- env -- rockcraft pack -v --platform='${{ matrix.platform.name }}'
      - name: Rockcraft logs
        if: ${{ success() || (failure() && steps.pack.outcome == 'failure') }}
        run: cat ~/.local/state/rockcraft/log/*
      - name: lxc image list --all-projects
        if: ${{ success() || (failure() && steps.pack.outcome == 'failure') }}
        run: sudo --user "$USER" --preserve-env --preserve-env=PATH -- env -- lxc image list --all-projects
      - run: touch .empty
      - name: Compute path in artifact
        id: path-in-artifact
        run: compute-path-in-artifact '${{ inputs.path-to-rock-directory }}'
      - name: Upload rock package
        uses: actions/upload-artifact@v5
        with:
          name: ${{ inputs.artifact-prefix }}-${{ steps.path-in-artifact.outputs.path }}--platform-${{ matrix.platform.name }}
          # .empty file required to preserve directory structure
          # See https://github.com/actions/upload-artifact/issues/344#issuecomment-1379232156
          path: |
            ${{ inputs.path-to-rock-directory }}/*.rock
            .empty
          include-hidden-files: true  # For `.empty`
          if-no-files-found: error
