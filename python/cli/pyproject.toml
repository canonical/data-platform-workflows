[tool.poetry]
name = "cli"
version = "0.1.0"
description = ""
license = "Apache-2.0"
authors = ["Carl Csaposs <carl.csaposs@canonical.com>"]
readme = "README.md"

[tool.poetry.scripts]
collect-charms = "cli.collect_charms:main"
release-charm = "cli.release_charm:main"
update-bundle = "cli.update_bundle:main"

[tool.poetry.dependencies]
python = "^3.10"
pyyaml = "^6.0.1"
requests = "^2.31.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
